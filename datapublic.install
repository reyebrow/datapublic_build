<?php

function datapublic_install() {

  // Update the superfish block title so that it doesn't show up
  /*$superfish = db_update('block')
  ->fields(array('title' => '<none>'))
  ->condition('theme', 'datapublic_theme')
  ->condition('module', 'superfish')
  ->condition('delta', '4')
  ->execute();*/

}